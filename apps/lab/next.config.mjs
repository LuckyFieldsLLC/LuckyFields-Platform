/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    transpilePackages: ['@luckyfields/blobs-utils', '@luckyfields/functions-utils'],
};

export default nextConfig;
